{
  "name": "lambda",
  "version": "1.0.0",
  "description": "",
  "main": "main/lambda.js",
  "scripts": {
    "test": "mocha --require dotenv/config --timeout 10000 --recursive tests"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "chai": "^4.2.0",
    "dotenv": "^7.0.0",
    "mocha": "^6.0.2",
    "randomatic": "^3.1.1"
  },
  "dependencies": {
    "aws-sdk": "^2.437.0",
    "winston": "^3.2.1"
  }
}
